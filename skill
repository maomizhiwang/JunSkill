---
name: coding-discipline
description: "在代码编写、Bug 修复、功能校验、重构、测试补充、文件编辑或评审修改时使用；强调编码前明确假设和歧义、优先简洁实现、精准限定改动范围、以成功标准和验证结果驱动执行。"
---

# Coding Discipline

使用本技能时，把任务转化为可落地、可验证、改动范围清晰的工程动作。优先遵守用户当前指令、项目已有约定和仓库现有风格；发现冲突或信息不足时，先说明问题并请求澄清。

## 执行顺序

1. 明确成功标准：在动手前说明本次任务完成后应满足什么条件，以及将如何验证。
2. 判断歧义和假设：列出关键假设；存在多种合理解释时列出选项并询问，不默默选定单一方案。
3. 选择最简单方案：如果有明显更简单的做法，直接指出并优先采用。
4. 精准修改代码：只改与当前任务直接相关的内容，匹配项目现有风格。
5. 验证结果：按任务类型运行对应测试、检查或手动验证，并报告结果。

## 编码前思考

- 明确假设，不确定时询问而非猜测。
- 存在歧义时，列出多种解释，不默默选定单一方案。
- 如果任务有明显更简单的做法，直接指出优化思路。
- 发现代码矛盾、逻辑不一致时及时暂停，请求信息澄清。
- 在开始复杂任务前输出简短执行计划，并为每一步标注验证方式。
- 简单、单点修改不输出冗长计划，直接说明成功标准并执行。

## 简洁优先

- 用最少的代码解决问题，拒绝冗余实现。
- 不为一次性需求创建抽象层、复杂架构。
- 不盲目增加扩展性、可配置性，应对“未来可能用到”的场景。
- 若代码可大幅精简，主动重写优化。
- 校验标准：以资深工程师视角判断，代码若过于复杂，立即简化。
- 新增依赖、公共工具、配置项前，先确认它们确实降低当前复杂度或符合项目既有模式。

## 精准修改

- 仅修改与当前任务直接相关的代码内容。
- 不顺手优化相邻代码、注释、排版格式。
- 不重构原本可以正常运行的代码模块。
- 严格匹配项目现有代码风格，保留原有编码习惯。
- 因本次修改产生的无效导入、废弃变量，可直接删除。
- 发现项目中原有的死代码、冗余内容，仅做文字提醒，不擅自删除。
- 遇到用户已有改动时，先读取并理解，基于现有内容继续工作，不回滚无关改动。

## 目标驱动执行

- 执行任务前，定义清晰、可落地的成功标准。
- 将“修复 Bug”转化为：编写用例复现问题，再调试至用例正常通过。
- 将“新增校验功能”转化为：针对异常输入编写测试用例，保证全部通过。
- 将“代码重构”转化为：完成重构后，确保原有所有测试用例正常运行。
- 多步骤复杂任务，先输出简短执行计划，同时标注每一步的验证方式。
- 如果无法编写自动化测试，说明原因，并用最小可行的手动验证、日志、命令输出或复现步骤替代。

## 文件编码

- 所有新增或修改文件必须使用 UTF-8 无 BOM。
- 禁止写入 UTF-16、GBK 或其它非 UTF-8 编码。
- 中文内容直接输出为中文字符，不使用 `\uXXXX` 转义。
- 写文件时使用能保证 UTF-8 无 BOM 的编辑方式；提交前如有风险，检查文件编码。

## 验证要求

- 优先运行项目已有的最小相关测试。
- 修改共享逻辑、接口契约或用户可见流程时，扩大验证范围。
- 验证失败时先定位原因；若失败与本次改动无关，明确说明证据和残余风险。
- 最终回复只报告关键改动、验证结果和必要风险，不展开无关细节。
